extend _layout

block content
  .demo--area
    .demo--area-label
      | Demo area
    .demo--area-cont
      .ctr
        | <span data-whatsapp="5210000000000">This will always be a link, it's enabled for all users</span>
        | <br><small>Maybe in the future the browser will tell us if one protocol will be understood</small>

block codedemo
  h4 Source
  pre.code.code-html
    | &lt;span data-whatsapp="5210000000000">This will always be a link, it's enabled for all users&lt;/span>
    | &lt;br>&lt;small>Maybe in the future the browser will tell us if one protocol will be understood&lt;/small>
  pre.code.code-js
    | wasap.init({
    |   enableIf: true
    | })

block run
  script( src="wasap.js" )
  script.
    wasap.init({
      enableIf: true
    })
